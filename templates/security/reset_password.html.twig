{% extends 'base.html.twig' %}

{% block title %}
	SnowTricks | Réinitialisation du mot de passe
{% endblock %}

{% block body %}
	<div class="row justify-content-center">
		<div class="col-md-6">
			<div class="text-center mb-5">
				<h1 class="fw-bolder">Réinitialisation du mot de passe</h1>
			</div>
			<div class="card rounded-3 py-5 px-4 px-md-5 mb-5">
				<div class="card-body">
					<div class="row gx-5 justify-content-center">
						<div class="col-10">
							{{ form_start(resetPasswordForm) }}
							<div class="form-floating mb-3">
								{{ form_widget(resetPasswordForm.username) }}
								{{ form_label(resetPasswordForm.username) }}
								{{ form_errors(resetPasswordForm.username) }}
							</div>
							<div class="form-floating mb-3">
								{{ form_widget(resetPasswordForm.password) }}
								{{ form_label(resetPasswordForm.password) }}
								{{ form_errors(resetPasswordForm.password) }}
							</div>
							<div class="d-grid">
								<button class="btn btn-primary btn-lg" id="submitButton" type="submit">Enregistrer</button>
							</div>
							{{ form_end(resetPasswordForm) }}
						</div>
					</div>
				</div>
			</div>
		</div>
	</div>
{% endblock %}
