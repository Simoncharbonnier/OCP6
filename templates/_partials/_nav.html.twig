<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
	<div class="container">
		<a class="navbar-brand" href="{{ path('app_home') }}">SnowTricks</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{ path('app_home') }}#tricks">Figures</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_trick_new') }}">Ajouter une figure</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_user', { 'username': app.user.username }) }}">Profil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_register') }}">Inscription</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('app_login') }}">Connexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
	<div class="container">
		<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
			<div class="d-flex align-items-center justify-content-around">
				<li class="nav-item">
					<a class="nav-link icon" href="{{ path('app_home') }}">
						<img src="/assets/img/icons/home.svg" alt="home">
					</a>
				</li>
				{% if app.user %}
					<li class="nav-item">
						<a class="nav-link icon" href="{{ path('app_trick_new') }}">
							<img src="/assets/img/icons/add_white.svg" alt="add">
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link icon" href="{{ path('app_user', { 'username': app.user.username }) }}">
							<img src="/assets/img/icons/profile.svg" alt="profile">
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link icon" href="{{ path('app_logout') }}">
							<img src="/assets/img/icons/logout.svg" alt="logout">
						</a>
					</li>
				{% else %}
					<li class="nav-item">
						<a class="nav-link icon" href="{{ path('app_register') }}">
							<img src="/assets/img/icons/register.svg" alt="register">
						</a>
					</li>
					<li class="nav-item">
						<a class="nav-link icon" href="{{ path('app_login') }}">
							<img src="/assets/img/icons/login.svg" alt="login">
						</a>
					</li>
				{% endif %}
			</div>
		</ul>
	</div>
</nav>
